{{>header}}
<div class="col-md-6 text-center col-lg">
    <img class="profile-img" src="./images/user.png" />
    <div class="profile-info">
        <p class="infoType">Watched movies 0.</p>
        {{!-- <p class="infoType">Watched movies {{numberOfEvents}}.</p> --}}
        {{!-- {{#each events}}
        <p>{{name}}</p>
        {{else}}
        <p>Just click "seen"</p>
        {{/each}} --}}
    </div>
</div>
{{>footer}}